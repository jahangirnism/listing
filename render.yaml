services:
  - type: web
    name: jna-listing-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120 --worker-class gevent --workers 1
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
